name: node.js test

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-run-server:
    runs-on: ubuntu-latest
    steps:
      - name: pull
        uses: actions/checkout@v4
        with:
          repository: mind4yottadb/mind-demo-server
          ref: main
          path: /home/runner/work/_temp

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: build-and-execute-server+test
        run: |
          cd /home/runner/work/_temp
          docker image build --build-arg SERVERMODE=client-test --build-arg BRANCH=main --progress=plain -t mind-server .
          docker run -d --init --tty -p 10000:10000 --name=mind-server mind-server
          cd /home/runner/work/mind4yottadb/mind-nodejs/mind-nodejs
          ls -la
          npm install
          npm test
